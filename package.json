{
  "name": "beyond-liveview-training",
  "version": "1.0.0",
  "description": "Training slides for 2023 Elixirconf",
  "main": "index.js",
  "scripts": {
    "start": "npm run watch & live-server .",
    "watch": "marp --watch index.md -o index.html",
    "livestate:watch": "marp --watch live_state.md -o live_state.html",
    "build": "marp index.md -o index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@marp-team/marp-cli": "^3.1.0",
    "live-server": "^1.2.2"
  }
}
